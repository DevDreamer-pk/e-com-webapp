import React from "react";
import Content from "./content";
// import { userService } from "../../services"; 

const SignIn = () => {

  // const signInResponse = async (email, password) => {
  //   const response = await userService.signIn(email, password);
  //   return response;
  // }

  return <Content />;
};

export default SignIn;
